# Локальное развертывание платформы Лицей №1

Это руководство поможет вам развернуть платформу Лицей №1 на вашем домашнем компьютере для разработки и тестирования.

## Требования

### Системные требования
- **Операционная система**: Linux, macOS, или Windows 10/11
- **RAM**: Минимум 4 ГБ, рекомендуется 8 ГБ
- **Дисковое пространство**: Минимум 5 ГБ свободного места
- **Процессор**: Dual-core или лучше

### Необходимое программное обеспечение
1. **Node.js** (версия 18 или выше)
   - Скачать: https://nodejs.org/
   - Проверка версии: `node --version`

2. **npm** или **bun** (менеджер пакетов)
   - npm идет вместе с Node.js
   - bun (опционально, быстрее): https://bun.sh/
   - Проверка версии: `npm --version` или `bun --version`

3. **Supabase CLI**
   ```bash
   npm install -g supabase
   ```
   - Проверка: `supabase --version`

4. **Docker** (для локального Supabase)
   - Скачать: https://www.docker.com/products/docker-desktop
   - Убедитесь, что Docker запущен перед началом

5. **Git** (для клонирования проекта)
   - Скачать: https://git-scm.com/
   - Проверка: `git --version`

## Шаг 1: Клонирование проекта

```bash
# Клонируйте репозиторий (или скачайте ZIP с Lovable)
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd lyceum-1

# Или если скачали ZIP
unzip lyceum-1.zip
cd lyceum-1
```

## Шаг 2: Установка зависимостей

```bash
# Используя npm
npm install

# Или используя bun (быстрее)
bun install
```

## Шаг 3: Настройка локального Supabase

### 3.1 Инициализация Supabase

```bash
# Инициализировать Supabase проект
supabase init

# Запустить локальный Supabase (требуется Docker)
supabase start
```

После запуска вы увидите локальные учетные данные:
```
API URL: http://localhost:54321
GraphQL URL: http://localhost:54321/graphql/v1
DB URL: postgresql://postgres:postgres@localhost:54322/postgres
Studio URL: http://localhost:54323
Anon key: eyJh... (длинный ключ)
Service Role key: eyJh... (длинный ключ)
```

**Сохраните эти данные!**

### 3.2 Применение миграций базы данных

```bash
# Применить все миграции
supabase db push
```

Это создаст все необходимые таблицы и политики RLS.

### 3.3 Создание файла .env

Создайте файл `.env` в корне проекта:

```bash
# Frontend URL
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_PUBLISHABLE_KEY=<ваш_anon_key_из_supabase_start>
VITE_SUPABASE_PROJECT_ID=local
```

## Шаг 4: Настройка Telegram бота

### 4.1 Создание бота в Telegram

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например, "Лицей 1 Бот")
   - Введите username бота (например, "lyceum1_dev_bot")
4. Сохраните **Bot Token**, который вам даст BotFather

### 4.2 Настройка секретов Edge Functions

Создайте файл `.env.local` в папке `supabase/`:

```bash
# supabase/.env.local
TELEGRAM_BOT_TOKEN=<ваш_telegram_bot_token>
LOVABLE_API_KEY=<ваш_lovable_api_key_или_оставьте_пустым>
ADMIN_SECRET_KEY=<любая_длинная_случайная_строка>
```

**Важно**: Для локальной разработки `LOVABLE_API_KEY` можно оставить пустым - RAG будет возвращать заглушки.

### 4.3 Настройка Webhook

Для локального тестирования вам понадобится инструмент для проброса портов, например **ngrok**:

```bash
# Установите ngrok
# Windows (через chocolatey): choco install ngrok
# macOS: brew install ngrok
# Linux: snap install ngrok

# Запустите ngrok
ngrok http 54321
```

Ngrok даст вам публичный URL, например: `https://abc123.ngrok.io`

Установите webhook для вашего бота:

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://abc123.ngrok.io/functions/v1/telegram-webhook"}'
```

**Альтернатива**: Для локальной разработки можно использовать polling вместо webhook (см. документацию Telegram Bot API).

## Шаг 5: Развертывание Edge Functions

### 5.1 Локальное развертывание

```bash
# Развернуть все Edge Functions локально
supabase functions deploy telegram-bot
supabase functions deploy telegram-webhook
supabase functions deploy verify-init-data
supabase functions deploy admin-login
supabase functions deploy create-admin
supabase functions deploy set-admin
```

### 5.2 Тестирование функций

```bash
# Проверить, что функции работают
supabase functions list
```

## Шаг 6: Создание первого администратора

### 6.1 Используя CLI скрипт

```bash
# Создать админского пользователя
node cli-create-admin.js admin_username secure_password "Администратор Иванов"
```

### 6.2 Через Edge Function

```bash
curl -X POST "http://localhost:54321/functions/v1/create-admin" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "SecurePass123!",
    "full_name": "Главный Администратор",
    "secret_key": "<ваш_ADMIN_SECRET_KEY>"
  }'
```

## Шаг 7: Запуск фронтенда

```bash
# Запустить dev-сервер
npm run dev

# Или с bun
bun dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

## Шаг 8: Настройка Telegram Mini App

1. В BotFather установите Mini App URL:
   ```
   /newapp
   ```
   - Выберите вашего бота
   - Введите название Mini App
   - Введите описание
   - Загрузите иконку (512x512 px)
   - **Web App URL**: Для локальной разработки используйте ngrok URL вашего фронтенда

2. Или добавьте кнопку в бот:
   ```
   /setmenubutton
   ```
   - Выберите вашего бота
   - Введите текст кнопки: "Открыть приложение"
   - Введите URL: `https://your-ngrok-url.ngrok.io`

## Шаг 9: Тестирование

1. **Откройте вашего бота в Telegram**
2. **Отправьте `/start`** - бот должен ответить приветствием
3. **Пройдите регистрацию** - выберите роль и категории новостей
4. **Откройте Mini App** - должна загрузиться страница логина
5. **Войдите через Telegram** - проверьте, что все работает

## Шаг 10: Заполнение базы данных

### 10.1 Через админ-панель

1. Перейдите на `/admin/login`
2. Войдите с учетными данными администратора
3. Заполните:
   - **Категории новостей** (Учеба, Спорт, Мероприятия и т.д.)
   - **Новости** (создайте несколько тестовых новостей)
   - **Базу знаний** (FAQ, информация о лицее)
   - **Шаблоны мероприятий**
   - **Мероприятия**

### 10.2 Через Supabase Studio

1. Откройте `http://localhost:54323`
2. Перейдите в **Table Editor**
3. Вручную добавьте данные в таблицы

### 10.3 Используя SQL

```bash
# Откройте SQL редактор
supabase db shell

# Выполните SQL команды, например:
INSERT INTO news_categories (name, icon, description) VALUES 
  ('Учеба', '📚', 'Новости об учебе'),
  ('Спорт', '⚽', 'Спортивные события'),
  ('Мероприятия', '🎉', 'Школьные мероприятия');
```

## Остановка сервисов

```bash
# Остановить фронтенд
Ctrl + C в терминале где запущен npm/bun dev

# Остановить Supabase
supabase stop

# Остановить ngrok
Ctrl + C в терминале где запущен ngrok
```

## Troubleshooting

### Docker не запускается
- Убедитесь, что Docker Desktop запущен
- На Windows: включите WSL 2
- Проверьте, что у Docker достаточно ресурсов (Settings → Resources)

### Supabase не стартует
```bash
# Сбросить Supabase
supabase stop --no-backup
supabase start
```

### Edge Functions не работают
```bash
# Проверить логи
supabase functions serve --debug

# Передеплоить функцию
supabase functions deploy <function-name>
```

### Telegram бот не отвечает
1. Проверьте, что webhook установлен правильно:
   ```bash
   curl "https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo"
   ```
2. Проверьте логи Edge Function:
   ```bash
   supabase functions logs telegram-webhook
   ```
3. Убедитесь, что ngrok работает

### База данных пустая
```bash
# Применить миграции заново
supabase db reset
supabase db push
```

## Полезные команды

```bash
# Посмотреть логи Supabase
supabase logs

# Посмотреть статус
supabase status

# Сбросить базу данных
supabase db reset

# Создать новую миграцию
supabase migration new <migration_name>

# Посмотреть логи конкретной функции
supabase functions logs <function-name> --tail

# Выполнить SQL запрос
supabase db shell
```

## Дополнительная информация

- **Документация Supabase CLI**: https://supabase.com/docs/guides/cli
- **Telegram Bot API**: https://core.telegram.org/bots/api
- **Telegram Mini Apps**: https://core.telegram.org/bots/webapps
- **Lovable Docs**: https://docs.lovable.dev/

## Следующие шаги

После успешного локального развертывания:
1. Изучите код в папках `src/` и `supabase/functions/`
2. Настройте RLS политики под ваши нужды
3. Добавьте дополнительные функции
4. Протестируйте безопасность
5. Подготовьтесь к продакшн развертыванию (см. DEPLOYMENT_FULL.md)

## Безопасность

**Важно для локальной разработки**:
- Не используйте простые пароли даже локально
- Не коммитьте файлы `.env` и `.env.local`
- Регулярно обновляйте зависимости
- Используйте разные токены для разработки и продакшна
- Не публикуйте ngrok URL в открытый доступ

---

**Успехов в разработке! 🚀**
