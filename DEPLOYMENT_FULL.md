# Развертывание системы "Лицей №1"

## Обзор архитектуры

Система состоит из 4 основных компонентов:

1. **Telegram Bot** - обрабатывает команды, регистрацию пользователей, RAG-ответы
2. **Telegram Mini App** - веб-приложение для просмотра новостей и мероприятий
3. **Admin Panel** - веб-панель для управления контентом
4. **Supabase Backend** - база данных и Edge Functions

## 1. Развертывание на Lovable Cloud (рекомендуется)

### Шаг 1: Публикация проекта

1. Нажмите кнопку **"Publish"** в Lovable
2. Получите URL проекта (например, `https://your-app.lovable.app`)

### Шаг 2: Настройка секретов

Добавьте следующие секреты в Settings → Integrations → Lovable Cloud:

- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `ADMIN_SECRET_KEY` - секретный ключ для создания админов (придумайте сложный)

### Шаг 3: Получение URL Edge Functions

Edge Functions автоматически развернуты по адресам:
- `https://<PROJECT_ID>.supabase.co/functions/v1/telegram-bot`
- `https://<PROJECT_ID>.supabase.co/functions/v1/verify-init-data`
- `https://<PROJECT_ID>.supabase.co/functions/v1/admin-login`
- `https://<PROJECT_ID>.supabase.co/functions/v1/create-admin`

Найдите ваш PROJECT_ID в .env файле проекта

### Шаг 4: Настройка Telegram бота

1. **Установить webhook:**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://yffdyyjugrzyqdvtjnho.supabase.co/functions/v1/telegram-bot"
  }'
```

2. **Настроить Mini App через BotFather:**

```
/setmenubutton
<ваш бот>
<название кнопки: "Открыть приложение">
<URL: https://your-app.lovable.app>
```

3. **Добавить команды:**

```
/setcommands
start - Начать работу с ботом
help - Помощь
webapp - Открыть Mini App
```

### Шаг 5: Создание первого администратора

Используйте CLI скрипт или curl:

**Вариант A: curl**
```bash
curl -X POST "https://yffdyyjugrzyqdvtjnho.supabase.co/functions/v1/create-admin" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "your_secure_password",
    "full_name": "Главный администратор",
    "secret_key": "YOUR_ADMIN_SECRET_KEY"
  }'
```

**Вариант B: Node.js скрипт**
```bash
node cli-create-admin.js admin your_secure_password "Главный администратор"
```

### Шаг 6: Тестирование

1. Откройте бот в Telegram и отправьте `/start`
2. Пройдите регистрацию (выберите роль и подписки)
3. Откройте Mini App через бот
4. Войдите в админ-панель: `https://your-app.lovable.app/admin/login`

---

## 2. Развертывание на других Serverless хостингах

### Vercel

1. **Подготовка репозитория:**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-repo-url>
git push -u origin main
```

2. **Импорт в Vercel:**
- Зайдите на [vercel.com](https://vercel.com)
- Нажмите "New Project" → Import Git Repository
- Выберите ваш репозиторий

3. **Настройка переменных окружения:**

В Vercel Settings → Environment Variables добавьте:
```
VITE_SUPABASE_URL=https://yffdyyjugrzyqdvtjnho.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<your-key>
```

4. **Деплой Edge Functions:**

Supabase Edge Functions должны быть развернуты отдельно на Supabase (см. ниже).

### Netlify

1. **Создание `netlify.toml`:**
```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

2. **Деплой:**
```bash
netlify init
netlify deploy --prod
```

3. **Переменные окружения:**

В Netlify Dashboard → Site settings → Environment variables

### Railway

1. **Создание `railway.toml`:**
```toml
[build]
  builder = "nixpacks"

[deploy]
  startCommand = "npm run preview"
```

2. **Деплой:**
```bash
railway login
railway init
railway up
```

---

## 3. Развертывание Supabase Backend

### Самостоятельная установка Supabase CLI

1. **Установка:**
```bash
# macOS
brew install supabase/tap/supabase

# Windows
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase

# Linux
brew install supabase/tap/supabase
```

2. **Инициализация проекта:**
```bash
supabase init
supabase login
supabase link --project-ref yffdyyjugrzyqdvtjnho
```

3. **Деплой миграций:**
```bash
supabase db push
```

4. **Деплой Edge Functions:**
```bash
# Установить секреты
supabase secrets set TELEGRAM_BOT_TOKEN=your_token
supabase secrets set ADMIN_SECRET_KEY=your_secret
supabase secrets set LOVABLE_API_KEY=your_key

# Деплой функций
supabase functions deploy telegram-bot
supabase functions deploy verify-init-data
supabase functions deploy admin-login
supabase functions deploy create-admin
supabase functions deploy set-admin
```

---

## 4. Управление системой

### Создание категорий новостей

Через админ-панель или SQL:

```sql
INSERT INTO news_categories (name, description, icon) VALUES
('Объявления', 'Важные объявления администрации', '📢'),
('События', 'Мероприятия и события лицея', '🎉'),
('Успехи', 'Достижения учеников', '🏆'),
('Расписание', 'Изменения в расписании', '📅');
```

### Создание шаблонов мероприятий

```sql
INSERT INTO event_templates (name, description, required_participants, required_channel_subscribers, reward_description, icon, color) VALUES
('Конкурс подписчиков', 'Соревнование за подписчиков канала', NULL, 100, 'Сертификат участника', '📱', '#3b82f6'),
('Спортивное соревнование', 'Спортивные состязания', 20, NULL, 'Медаль победителя', '🏅', '#10b981'),
('Олимпиада', 'Предметная олимпиада', 50, NULL, 'Грамота', '📚', '#f59e0b');
```

### Наполнение базы знаний

Через админ-панель добавьте информацию о:
- Расписании и режиме работы
- Правилах поведения
- Контактах администрации
- Часто задаваемых вопросах
- История лицея
- Преподавательский состав

---

## 5. Безопасность

### Важные меры безопасности:

1. **Использование HTTPS** - обязательно для всех веб-хуков
2. **Проверка initData** - реализована в `verify-init-data` функции
3. **Row Level Security (RLS)** - настроена для всех таблиц
4. **Секреты** - храните в Supabase Secrets, не в коде
5. **Админ-пароли** - используйте bcrypt хеширование (реализовано)
6. **Rate Limiting** - Lovable AI имеет встроенные лимиты

### Рекомендации:

- Регулярно обновляйте зависимости
- Используйте сильные пароли для админов
- Мониторьте логи Edge Functions
- Делайте бэкапы базы данных

---

## 6. Мониторинг и отладка

### Просмотр логов Edge Functions:

```bash
supabase functions logs telegram-bot
supabase functions logs verify-init-data
```

### Проверка базы данных:

```bash
supabase db inspect
```

### Тестирование webhook:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

---

## 7. Troubleshooting

### Проблема: Webhook не работает

**Решение:**
- Проверьте URL webhook: должен быть HTTPS
- Проверьте логи: `supabase functions logs telegram-bot`
- Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен

### Проблема: Пользователи не могут войти

**Решение:**
- Проверьте, что `verify-init-data` функция развернута
- Проверьте логи авторизации в Supabase
- Убедитесь, что RLS policies настроены правильно

### Проблема: RAG не работает

**Решение:**
- Проверьте, что `LOVABLE_API_KEY` установлен
- Проверьте, что база знаний заполнена
- Проверьте логи `telegram-bot` функции

### Проблема: Admin панель недоступна

**Решение:**
- Убедитесь, что создан хотя бы один admin пользователь
- Проверьте, что `admin-login` функция развернута
- Проверьте правильность логина/пароля

---

## 8. Масштабирование

### Повышение производительности:

1. **Индексы базы данных:**
```sql
CREATE INDEX idx_news_published ON news(is_published, published_at);
CREATE INDEX idx_events_active ON events(is_active, start_date);
CREATE INDEX idx_user_preferences ON user_preferences(user_id, category_id);
```

2. **Кеширование:**
- Используйте CDN для статических ресурсов
- Кешируйте запросы к новостям на фронтенде

3. **Rate Limiting:**
- Реализуйте rate limiting для Edge Functions при необходимости

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи Edge Functions
2. Изучите документацию Supabase и Telegram API
3. Обратитесь к сообществу Lovable
4. Свяжитесь с поддержкой Supabase для проблем с БД

---

## Чек-лист развертывания

- [ ] Supabase проект настроен
- [ ] Edge Functions развернуты
- [ ] Секреты установлены
- [ ] Telegram Bot Token получен
- [ ] Webhook настроен
- [ ] Mini App URL добавлен в бота
- [ ] Первый администратор создан
- [ ] Категории новостей добавлены
- [ ] Шаблоны мероприятий созданы
- [ ] База знаний заполнена
- [ ] Система протестирована

---

**Успешного развертывания! 🚀**